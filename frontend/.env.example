# Copie: cp .env.example .env   (não commite o .env)
#
# Dev (npm run dev): o Vite faz proxy de /api → http://localhost:8000
# Prod Docker: o Dockerfile usa ARG VITE_API_BASE_URL=/api (este arquivo
# não entra na imagem). Não precisa de frontend/.env na VPS.

VITE_API_BASE_URL=/api
